{{- if gt (int .Values.replicaCount) 1 }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "fuellhorn.fullname" . }}
  labels:
    {{- include "fuellhorn.labels" . | nindent 4 }}
spec:
  minAvailable: 1
  selector:
    matchLabels:
      {{- include "fuellhorn.selectorLabels" . | nindent 6 }}
{{- end }}
